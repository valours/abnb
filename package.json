{
  "scripts": {
    "client": "cd web-client && yarn start",
    "server": "cd server-api && yarn start:dev",
    "start": "concurrently \"npm run client\" \"npm run server\""
  },
  "dependencies": {
    "concurrently": "^4.1.0"
  }
}
